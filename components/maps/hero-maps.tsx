import { HeroWrapper } from "../share/hero-wrapper";

export const HeroMaps = () => {
  return (
    <section>
      <HeroWrapper>
        <h2 className="text-white text-3xl md:text-8xl font-bold  leading-tight max-w-2xl text-center">
          Municipios con <span className="text-fourth">AVGM</span> en Durango
        </h2>
      </HeroWrapper>
    </section>
  );
};
